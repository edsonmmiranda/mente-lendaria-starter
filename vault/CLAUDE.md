# Meu Segundo Cérebro

Leia `AGENTS.md` em toda sessão e `INICIE-AQUI-IA.md` antes da primeira configuração.

As skills compatíveis com Claude Code vivem em `.claude/skills/` e são projeções idênticas da fonte canônica `.agents/skills/`.

Use as ferramentas de arquivo do próprio Claude. Não exija Python, Node.js, Git, shell ou outro ambiente instalado pela pessoa. Sem ferramentas reais para ler, copiar e escrever arquivos, responda `BLOQUEADO: acesso local indisponível`. Nunca simule execução. Só declare sucesso depois de criar e reler `.segundo-cerebro-kit/completion-receipt.json` com `status: complete`.
